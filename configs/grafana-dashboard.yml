# Grafana Dashboard Provisioning Configuration
# This file tells Grafana where to find dashboard JSON files

apiVersion: 1

providers:
  - name: 'CoW Performance Testing'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/dashboards
